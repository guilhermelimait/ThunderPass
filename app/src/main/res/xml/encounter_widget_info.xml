<?xml version="1.0" encoding="utf-8"?>
<!--
    AppWidgetProviderInfo for the EncounterWidget.
    minWidth / minHeight: 2×2 cells (110dp × 110dp per cell baseline = 110×110 min).
    previewLayout: a static layout shown in the widget picker.
    updatePeriodMillis: 0 = app handles updates; we push on new encounters.
-->
<appwidget-provider
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:minWidth="180dp"
    android:minHeight="110dp"
    android:minResizeWidth="110dp"
    android:minResizeHeight="110dp"
    android:resizeMode="horizontal|vertical"
    android:targetCellWidth="2"
    android:targetCellHeight="2"
    android:updatePeriodMillis="0"
    android:initialLayout="@layout/encounter_widget_loading"
    android:widgetCategory="home_screen"
    android:widgetFeatures="reconfigurable|configuration_optional" />
